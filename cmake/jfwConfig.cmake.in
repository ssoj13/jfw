@PACKAGE_INIT@

# Find dependencies
include(CMakeFindDependencyMacro)

find_dependency(Threads REQUIRED)
find_dependency(fmt REQUIRED)
find_dependency(glm CONFIG REQUIRED)

# Find simplexnoise (if not using FetchContent)
find_dependency(simplexnoise QUIET)

# Maya is optional
find_dependency(Maya QUIET)

include("${CMAKE_CURRENT_LIST_DIR}/jfwTargets.cmake")

check_required_components(jfw)